import os

# ALIGNN CALCS

# open vasp files in vasp_files_temp
# run alignn-ff potential energy calculation

# look in vasp_files_temp for a file with a specific name

def calculate_energy(filename):
    print(filename)
    energy = 1
    return energy